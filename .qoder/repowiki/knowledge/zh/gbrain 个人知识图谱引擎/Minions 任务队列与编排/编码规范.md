- 引擎层的批量原语已内置 `withRetry`，Minions handler 禁止在外层再次包裹重试逻辑。
- Protected-name 任务（shell、synthesize、patterns、consolidate）只接受本地受信调用者，MCP 远程调用必须拒绝。
- Worker 进程退出码遵循 `worker-exit-codes.ts` 定义的分类，监督器据此决定是否重启。
- `scripts/check-worker-pool-atomicity.sh` 和 `scripts/check-worker-lock-renewal-shape.sh` 是 CI 守护脚本。