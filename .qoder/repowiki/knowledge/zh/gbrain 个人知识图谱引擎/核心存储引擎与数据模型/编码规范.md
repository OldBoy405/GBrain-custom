- 所有写入操作（如 `putPage`, `upsertChunks`）必须显式传递 `sourceId`，严禁依赖数据库默认的 'default' 值，以防止跨源数据污染。
- 批量写入原语（如 `addLinksBatch`）内部封装了基于 `withRetry` 的自动重试机制，调用方禁止在外层再次包裹重试逻辑以避免指数级放大负载。
- 软删除模式统一使用 `deleted_at` 时间戳字段，查询时默认附加 `AND deleted_at IS NULL` 过滤条件，仅当显式传入 `includeDeleted: true` 时才暴露已删除记录。