- **注册 OAuth 客户端**：`gbrain auth register-client --name <name> --scope <scope>`。
- **令牌管理**：`gbrain auth list-clients`、`gbrain auth revoke-client <id>`。
- **关键测试**：`test/oauth.test.ts`（完整 OAuth 流程）、`test/trust-boundary-contract.test.ts`（fail-closed 语义）、`test/file-upload-security.test.ts`（文件系统安全）。