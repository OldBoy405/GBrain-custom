- 所有涉及多源数据的引擎方法必须支持 `sourceId`（标量）或 `sourceIds`（数组）参数以明确数据边界。
- 批量写入原语（如 `addLinksBatch`）内部内置基于 `withRetry` 的 transient error 重试机制，禁止调用方在外层重复包装重试逻辑。
- CLI 命令与 MCP 工具共享同一套 `Operation` 定义与参数解析逻辑，通过 `formatResult` 统一输出格式化。