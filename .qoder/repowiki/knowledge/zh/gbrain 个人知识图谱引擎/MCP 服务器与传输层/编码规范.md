- `dispatch.ts` 是传输层的单一事实来源 — 新增参数校验或上下文构建逻辑必须在此处添加，不可在 server.ts 或 serve-http.ts 中重复。
- stdio 调用者始终 `remote=true`；`gbrain call <op>` 命令设置 `remote=false` 作为受信本地路径。
- HTTP 传输的 `submit_job` 对 protected-name 任务（shell、synthesize 等）必须拒绝远程调用。