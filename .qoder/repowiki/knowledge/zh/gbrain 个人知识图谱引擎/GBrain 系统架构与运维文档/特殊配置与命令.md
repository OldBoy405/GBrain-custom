- **初始化**：`gbrain init --pglite`（本地）或 `gbrain init --mcp-only`（瘦客户端）。
- **健康检查**：`gbrain doctor` 执行全链路诊断，包括检索反射心跳与同步失败账本检查。
- **迁移与升级**：`gbrain migrate --to supabase` 支持引擎切换；`gbrain upgrade` 处理 Schema 演进与分块器版本 bump。