- 核心测试流水线 (test.yml)：采用内容哈希缓存机制跳过未变更代码的测试，包含并行矩阵分片（10 shards）、串行测试、长耗时评估任务及静态检查。
- 分层 E2E 测试 (e2e.yml)：分为机械层（Tier 1）和 LLM 技能层（Tier 2），依赖 PostgreSQL 服务容器及外部 API 密钥。
- 重型测试隔离 (heavy-tests.yml)：通过 PR 标签或定时任务触发高资源消耗的性能与压力测试。
- 发布自动化 (release.yml)：监听版本标签，并行构建 macOS ARM64 与 Linux x64 二进制文件并创建 GitHub Release。