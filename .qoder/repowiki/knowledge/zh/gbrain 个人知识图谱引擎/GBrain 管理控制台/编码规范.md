- 所有技术标识符（如 Agent ID、Token、延迟数值）均使用 JetBrains Mono 等宽字体渲染以增强可读性。
- 界面元素严格遵循左对齐原则，表格数据、标题及表单标签均不使用居中对齐（空状态除外）。
- API 请求失败时采用静默处理或极简错误提示，避免使用加载骨架屏，优先展示缓存数据或空白状态。
- 语义化颜色仅用于 Badge 组件表示状态（如 success, danger, read, write），界面其余部分保持单色调。