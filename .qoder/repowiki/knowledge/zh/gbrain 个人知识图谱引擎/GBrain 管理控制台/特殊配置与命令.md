- **开发启动**：运行 `bun run dev` (或 `npm run dev`) 启动 Vite 开发服务器。
- **生产构建**：运行 `bun run build` 生成静态资源到 `dist/` 目录。
- **一致性校验**：通过 `scripts/check-admin-scope-drift.sh` 脚本确保前端 `scope-constants.ts` 与后端核心权限定义保持同步。