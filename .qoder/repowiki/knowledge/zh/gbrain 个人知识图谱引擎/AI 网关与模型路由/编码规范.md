- 网关通过 `configureGateway()` 一次性读取配置，调用时绝不读 `process.env`。
- 新提供商通过添加 Recipe 文件实现（`recipes/<provider>.ts`），注册到 `recipes/index.ts`。
- 定价更新只在 `model-pricing.ts` 中进行；其他定价表是派生视图，由漂移测试守护。
- AI HTTP 请求必须设置超时（默认通过 gateway 级 `fetchTimeoutMs`），防止挂起的提供商 socket 永久阻塞。