- 所有语音人格提示词必须包含强制性的音频输出指令，禁止生成纯文本或内部独白，以确保低延迟的语音交互体验。
- 工具调用结果必须封装为结构化信封（Structured Envelope），即使发生错误也不抛出异常，以防止语音通话中断。
- 技能包目录结构必须严格遵循约定，包含 `skills/`（定义与路由）、`test/`（单元与 E2E）、`evals/`（LLM 裁判）及 `runbooks/`（引导文档）。
- 系统提示词组装采用“身份优先”原则，将人格定义置于共享上下文之前，以防止模型在长对话中漂移为通用助手。