- **安装命令**：使用 `gbrain integrations install agent-voice --target <host-repo>` 将参考代码复制到宿主项目。
- **刷新机制**：通过 `--refresh` 标志执行三方分类（不变、过时、本地修改）以同步上游更新。
- **测试指令**：支持 `bun run test:e2e` 进行 WebRTC 往返测试，以及 `gbrain skillpack doctor` 对技能包进行合规性评分。